
services:
  # MySQL Database
  mysql-auth:
    image: mysql:8.0
    container_name: mysql-auth
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: auth_db
      MYSQL_USER: auth_user
      MYSQL_PASSWORD: auth_password
    ports:
      - "3306:3306"
    volumes:
      - mysql-auth-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped
  auth-service:
    build:
      context: ../../backend/auth-service
      dockerfile: Dockerfile
    container_name: auth-service
    networks:
      - elearning-network
    depends_on:
      mysql-auth:
        condition: service_healthy
    restart: unless-stopped

  nginx-auth:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-auth
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - auth-service
    restart: unless-stopped
  # --- User Service ---
  mysql-user:
    image: mysql:8.0
    container_name: mysql-user
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: user_db
      MYSQL_USER: user_user
      MYSQL_PASSWORD: user_password
    ports:
      - "3307:3306"   # port différent pour éviter conflit avec mysql-auth
    volumes:
      - mysql-user-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  user-service:
    build:
      context: ../../backend/user-service
      dockerfile: Dockerfile
    container_name: user-service
    volumes:
      - ./storage:/var/www/html/storage/app/public
    networks:
      - elearning-network
    depends_on:
      mysql-user:
        condition: service_healthy
    restart: unless-stopped

  nginx-user:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-user
    ports:
      - "8001:80"
    volumes:
      - ./nginx/user.conf:/etc/nginx/conf.d/default.conf
      - ./storage:/var/www/html/public/storage
    networks:
      - elearning-network
    depends_on:
      - user-service
    restart: unless-stopped

  phpmyadmin-user:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-user
    environment:
      PMA_HOST: mysql-user
      PMA_PORT: 3306
    ports:
      - "8081:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-user
    restart: unless-stopped
  # --- Course Service ---
    # --- Course Service ---
  mysql-course:
    image: mysql:8.0
    container_name: mysql-course
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: course_db
      MYSQL_USER: course_user
      MYSQL_PASSWORD: course_password
    ports:
      - "3308:3306"
    volumes:
      - mysql-course-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  course-service:
    build:
      context: ../../backend/course-service
      dockerfile: Dockerfile
    container_name: course-service
    networks:
      - elearning-network
    depends_on:
      mysql-course:
        condition: service_healthy
    restart: unless-stopped

  nginx-course:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-course
    ports:
      - "8002:80"
    volumes:
      - ./nginx/course.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - course-service
    restart: unless-stopped

  phpmyadmin-course:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-course
    environment:
      PMA_HOST: mysql-course
      PMA_PORT: 3306
    ports:
      - "8082:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-course
    restart: unless-stopped
  # --- Content Service ---
  mysql-content:
    image: mysql:8.0
    container_name: mysql-content
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: content_db
      MYSQL_USER: content_user
      MYSQL_PASSWORD: content_password
    ports:
      - "3310:3306"
    volumes:
      - mysql-content-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  content-service:
    build:
      context: ../../backend/content-service
      dockerfile: Dockerfile
    container_name: content-service
    networks:
      - elearning-network
    depends_on:
      mysql-content:
        condition: service_healthy
    restart: unless-stopped

  nginx-content:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-content
    ports:
      - "8003:80"
    volumes:
      - ./nginx/content.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - content-service
    restart: unless-stopped

  phpmyadmin-content:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-content
    environment:
      PMA_HOST: mysql-content
      PMA_PORT: 3306
    ports:
      - "8083:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-content
    restart: unless-stopped
  # --- Executor DB ---
  mysql-executor:
    image: mysql:8.0
    container_name: mysql-executor
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: executor_db
      MYSQL_USER: executor_user
      MYSQL_PASSWORD: executor_password
    ports:
      - "3313:3306"
    volumes:
      - mysql-executor-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  executor-service:
    build:
      context: ../../backend/executor-service
      dockerfile: Dockerfile
    container_name: executor-service
    depends_on:
      mysql-executor:
        condition: service_healthy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp/executor:/tmp/executor
    networks:
      - elearning-network
    restart: unless-stopped
    command: >
      sh -c "chmod 777 /tmp/executor && docker-php-entrypoint php-fpm" 

  nginx-executor:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-executor
    ports:
      - "8004:80"
    volumes:
      - ./nginx/executor.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - executor-service
    restart: unless-stopped
  # --- Notification Service ---
    # --- Notification Service ---
  mysql-notification:
    image: mysql:8.0
    container_name: mysql-notification
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: notification_db
      MYSQL_USER: notification_user
      MYSQL_PASSWORD: notification_password
    ports:
      - "3311:3306"
    volumes:
      - mysql-notification-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  notification-service:
    build:
      context: ../../backend/notification-service
      dockerfile: Dockerfile
    container_name: notification-service
    networks:
      - elearning-network
    depends_on:
      mysql-notification:
        condition: service_healthy
    restart: unless-stopped

  notification-worker:
    build:
      context: ../../backend/notification-service
      dockerfile: Dockerfile.worker
    container_name: notification-worker
    networks:
      - elearning-network
    depends_on:
      mysql-notification:
        condition: service_healthy
    restart: unless-stopped

  nginx-notification:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-notification
    ports:
      - "8006:80"
    volumes:
      - ./nginx/notification.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - notification-service
    restart: unless-stopped

  phpmyadmin-notification:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-notification
    environment:
      PMA_HOST: mysql-notification
      PMA_PORT: 3306
    ports:
      - "8086:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-notification
    restart: unless-stopped
  # --- Quiz Service ---
  mysql-quiz:
    image: mysql:8.0
    container_name: mysql-quiz
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: quiz_db
      MYSQL_USER: quiz_user
      MYSQL_PASSWORD: quiz_password
    ports:
      - "3312:3306"
    volumes:
      - mysql-quiz-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  quiz-service:
    build:
      context: ../../backend/quiz-service
      dockerfile: Dockerfile
    container_name: quiz-service
    networks:
      - elearning-network
    depends_on:
      mysql-quiz:
        condition: service_healthy
    restart: unless-stopped

  nginx-quiz:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-quiz
    ports:
      - "8005:80"
    volumes:
      - ./nginx/quiz.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - quiz-service
    restart: unless-stopped

  phpmyadmin-quiz:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-quiz
    environment:
      PMA_HOST: mysql-quiz
      PMA_PORT: 3306
    ports:
      - "8087:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-quiz
    restart: unless-stopped
  # phpMyAdmin (optional)
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-auth
    environment:
      PMA_HOST: mysql-auth
      PMA_PORT: 3306
    ports:
      - "8085:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-auth
    restart: unless-stopped
  gateway:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: gateway
    ports:
      - "8080:80"
    networks:
      - elearning-network
    depends_on:
      - nginx-auth
      - nginx-user
      - nginx-course
      - nginx-content
      - nginx-executor
      - nginx-messaging
      - nginx-forum   
    restart: unless-stopped
  mailpit:
    image: axllent/mailpit:latest
    container_name: mailpit
    ports:
      - "1025:1025"
      - "8025:8025"
    networks:
      - elearning-network
    restart: unless-stopped

  # --- Forum Service ---
  mysql-forum:
    image: mysql:8.0
    container_name: mysql-forum
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: forum_db
      MYSQL_USER: forum_user
      MYSQL_PASSWORD: forum_password
    ports:
      - "3314:3306"
    volumes:
      - mysql-forum-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  forum-service:
    build:
      context: ../../backend/forum-service
      dockerfile: Dockerfile
    container_name: forum-service
    networks:
      - elearning-network
    depends_on:
      mysql-forum:
        condition: service_healthy
    restart: unless-stopped

  nginx-forum:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-forum
    ports:
      - "8008:80"
    volumes:
      - ./nginx/forum.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - forum-service
    restart: unless-stopped

  phpmyadmin-forum:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-forum
    environment:
      PMA_HOST: mysql-forum
      PMA_PORT: 3306
    ports:
      - "8088:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-forum
    restart: unless-stopped

  # --- Messaging Service ---
  mysql-messaging:
    image: mysql:8.0
    container_name: mysql-messaging
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: messaging_db
      MYSQL_USER: messaging_user
      MYSQL_PASSWORD: messaging_password
    ports:
      - "3315:3306"
    volumes:
      - mysql-messaging-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  messaging-service:
    build:
      context: ../../backend/messaging-service
      dockerfile: Dockerfile
    container_name: messaging-service
    networks:
      - elearning-network
    depends_on:
      mysql-messaging:
        condition: service_healthy
    restart: unless-stopped

  nginx-messaging:
    build:
      context: ./nginx-secure
      dockerfile: Dockerfile
    container_name: nginx-messaging
    ports:
      - "8009:80"
    volumes:
      - ./nginx/messaging.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - messaging-service
    restart: unless-stopped

  phpmyadmin-messaging:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-messaging
    environment:
      PMA_HOST: mysql-messaging
      PMA_PORT: 3306
    ports:
      - "8089:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-messaging
    restart: unless-stopped
  chatbot-service:
    build:
      context: ../../backend/chatbot-service
      dockerfile: Dockerfile
    container_name: chatbot-service
    ports:
      - "8007:80"
    networks:
      - elearning-network
    restart: unless-stopped

  # --- Payment Service ---
  mysql-payment:
    image: mysql:8.0
    container_name: mysql-payment
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: payment_db
      MYSQL_USER: payment_user
      MYSQL_PASSWORD: payment_password
    ports:
      - "3316:3306"
    volumes:
      - mysql-payment-data:/var/lib/mysql
    networks:
      - elearning-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    restart: unless-stopped

  payment-service:
    build:
      context: ../../backend/payment-service
      dockerfile: Dockerfile
    container_name: payment-service
    networks:
      - elearning-network
    depends_on:
      mysql-payment:
        condition: service_healthy
    restart: unless-stopped

  nginx-payment:
    image: nginx:alpine
    container_name: nginx-payment
    ports:
      - "8010:80"
    volumes:
      - ./nginx/payment.conf:/etc/nginx/conf.d/default.conf
    networks:
      - elearning-network
    depends_on:
      - payment-service
    restart: unless-stopped

  phpmyadmin-payment:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin-payment
    environment:
      PMA_HOST: mysql-payment
      PMA_PORT: 3306
    ports:
      - "8090:80"
    networks:
      - elearning-network
    depends_on:
      - mysql-payment
    restart: unless-stopped

networks:
  elearning-network:
    driver: bridge

volumes:
  mysql-auth-data:
  mysql-user-data:
  mysql-course-data:
  mysql-content-data:
  mysql-notification-data:
  mysql-quiz-data:
  mysql-executor-data:
  mysql-forum-data:
  mysql-messaging-data:
  mysql-payment-data:
  user_storage:
